package com.historicalplaces.core;

/*
 * This class contains logout Servlet which performs user logout.
 * It ask AuthenticationAmanger if user is authorized to perform this operations
 * and if Yes then performs logout operation, session and 
 * cookies (if we will use them) invalidation and returns result to user.
 */
public class LogoutServlet {

}
